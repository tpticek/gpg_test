Hello,

This following are the Lithuania reusabilty metrics on {{ ansible_date_time.year }}/{{ ansible_date_time.month }}/{{ ansible_date_time.day }} at {{ ansible_date_time.hour }}h{{ ansible_date_time.minute }}min{{ ansible_date_time.second }}s CET:

{{ "\n".join(reusability_metrics.stdout_lines[1:]) | regex_replace("\(1 row\)","") }}

Best regards,
Telia EOC ECM DevOps
